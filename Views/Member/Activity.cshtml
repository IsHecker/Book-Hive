@model IEnumerable<ActivityLog>

<h4>Recent Activity</h4>
<ul class="list-group">
    @foreach (var activity in @Model)
    {
        <li class="list-group-item">
            You @activity.Action <strong>@activity.BookName</strong> @activity.ActionDetails
        </li>
    }
</ul>